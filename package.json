{
  "name": "@rexezuge/secrets-provider",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cf-typegen": "wrangler types",
    "predeploy": "npm install && npm run cf-typegen && npm run prettier && npm run lint && npm run tsc",
    "deploy": "wrangler deploy --dry-run",
    "postdeploy": "wrangler deploy",
    "deploy:fast": "wrangler deploy",
    "prettier": "prettier --write '!app' .",
    "lint": "eslint --ext .ts,.tsx --fix .",
    "tsc": "tsc --noemit"
  },
  "devDependencies": {
    "eslint": "^10.0.x",
    "eslint-config-prettier": "^10.1.x",
    "eslint-plugin-prettier": "^5.5.x",
    "globals": "^17.4.x",
    "jsonc-parser": "^3.3.x",
    "prettier": "^3.8.x",
    "typescript": "^5.9.x",
    "typescript-eslint": "^8.57.x",
    "wrangler": "^4.74.x"
  },
  "dependencies": {}
}
